define([
'jquery',
'underscore',
'backbone'
], function ($, _, BB) {
var QueryFragment = BB.Model.extend({

// idAttribute:,
// defaults:{},
initialize:function(attributes, options){},
// validate:function(attributes, options){},

});
return QueryFragment;
});
